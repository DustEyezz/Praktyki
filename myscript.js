function testResults (form) {
    var TestVar = form.inputbox.value;
    alert ("You typed: " + TestVar);
}

function testResults2 (form) {
    var TestVar = form.inputbox.value;
    TestVar = TestVar + " reee";
    alert ("You goofed: " + TestVar);
}